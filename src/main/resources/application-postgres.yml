spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/festival_copilot
    username: festival_user
    password: OOPS_123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode: always
  h2:
    console:
      enabled: false

festival:
  maps:
    provider: google